# Copy to .env and adjust. (.env is gitignored.)
DOMAIN=panel.example.com
ADMIN_USER=admin
ADMIN_PASS=change-me
# Used only to seed the admin account on a fresh/empty database; an existing DB
# keeps its current admin credentials.
